# Web app data source (Supabase). Optional: the app ships with the public
# project defaults baked in, so it works without this file. Override here to
# point the dashboard at a different project. Only the publishable/anon key
# belongs in the browser — never the service-role key.
VITE_SUPABASE_URL=https://urnnulxipkmkwpkhqkip.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_TSBB_g6z8Xgr4H3xp0Nqtg_FsyAm7NS
